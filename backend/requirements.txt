# CinemaList backend dependencies
# No versions pinned — pip will always install the latest stable release.
# If you ever need to freeze exact versions (e.g. for a server deploy),
# run: pip freeze > requirements-lock.txt

fastapi
uvicorn[standard]
sqlalchemy
alembic
python-dotenv
httpx
pydantic
pydantic-settings
python-multipart
aiofiles
Pillow
aiosqlite
